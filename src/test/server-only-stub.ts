/**
 * Test stub for Next.js's `server-only` package, which exists purely to make
 * client-side imports fail at build time and has no runtime module. Aliased in
 * vitest.config.ts so server modules can be unit-tested.
 */
export {};
